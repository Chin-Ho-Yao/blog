
services:
#  db:
#    image: mysql:8.0
#    container_name: mysql
#    environment:
#      MYSQL_ROOT_PASSWORD: Jackul6fupck6!
#      MYSQL_DATABASE: blog
#      MYSQL_USER: root
#      MYSQL_PASSWORD: Jackul6fupck6!
#
##    ports:
##      - "3306:3306"
#    network_mode: "host" # 將 network_mode 設置為 "host"
#    volumes:
#      - db_data:/var/lib/mysql

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: spring-boot-app
    environment:
#      SPRING_PROFILES_ACTIVE: dev

      # 直接把帳密寫在此
      SPRING_DATASOURCE_URL: jdbc:mysql://host.docker.internal:3306/blog?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: Jackul6fupck6!
    ports:
      - "8000:8080"
#    depends_on:
#      - db

volumes:
  db_data: